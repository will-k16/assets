var RiverObbyDeadPlayerController = pc.createScript('riverObbyDeadPlayerController');

RiverObbyDeadPlayerController.prototype.initialize = function () {
    /*
    this.marketController = this.app.root.findByName("Market").script.market;
    this.index = JSON.parse(Utils.getItem("saveInventory"));
    let skinMaterial;
    if (Utils.getItem("saveInventory")) {
        if (this.index[2])
            skinMaterial = this.marketController.skinMenu[this.index[2]];
        else
            skinMaterial = this.marketController.skinMenu[0];

    }
    else
        skinMaterial = this.marketController.skinMenu[0];
    let objects = this.entity.children[0].children;
    for (let i = 0; i < objects.length; i++) {
        objects[i].render.meshInstances[0].material = skinMaterial.material.resource;
    }
    */
};